// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface ExtractedContent {
  id: string;
  text: string;
  html: string;
  length: number;
  url: string;
  comments: Comment[];
  auditLogs: AuditLog[];
}
